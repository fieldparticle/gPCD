Document the cluster rule
Define the assumption: pair contacts are report edges, but a connected contact graph is one collision body for response.

Add cluster reporting
Capture files should show something like:

[cluster c0]
particles=0,1,2
contacts=0-1,0-2
phase=compression/rebound
p_start=(...)
p_current=(...)
ke_start=...
ke_current=...
That will make debugging much easier.

Define cluster internal momentum
Instead of storing only pair nimom, compute cluster-level stored/released internal momentum from all active contact normals.

Replace the clamp with a cluster rebound solve
Right now we compute provisional pair response then clamp it. Cleaner model: solve the cluster once from momentum, energy, and contact normals.

Then port to GLSL
Once the Python behavior is stable, mirror the same cluster structure in Vulkan.