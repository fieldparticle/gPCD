// ============================================================================
// BoundaryParticleReservoirHorizontal: reduced Python profile
// ============================================================================

// --------------------------------------------------------------------------
// Shared preview and Python game-camera bounds
// --------------------------------------------------------------------------
// PlotParticles uses all three axis ranges. SimulationRunner uses the x and y
// ranges first and falls back to legacy Wall* values only when these are absent.
// SimulationRunner then applies zoom around each axis-range center.
x_axis_lims = [0.0,30.0];
y_axis_lims = [0.0,10.0];
z_axis_lims = [0.0,3.0];

//  Distance between particle perimiters
particle_separation_distance 	= 1.0;
// Required: explicit cell counts for the cell-array domain.
cell_array_width 				= 33;
cell_array_height 				= 13;
cell_array_depth 				= 5;

view_center = [ 15,4,2];

// Frame when the piston begins moving.
piston_start_frame = 20;


//death_bounds = [
//    x_min, x_max,
//    y_min, y_max,
//    z_min, z_max
//];

death_bounds = [
    1, 30,
    1, 9,
    0, 4
];

initial_particle_velocity = [ 0.05, 0.0, 0.0 ];
piston_velocity = [ 0.05, 0.0, 0.0 ];

// 2D packing curves derive x/y packing bounds. Z is configured separately.
packing_z_bounds = [0, 3];

// Parametric polynomial wall segments.
// [ax, bx, cx, dx, ay, by, cy, dy, wall_flag, curve_role]
// x(t) = ax + bx*t + cx*t^2 + dx*t^3
// y(t) = ay + by*t + cy*t^2 + dy*t^3
// 0 <= t <= 1
// wall_flag: 3=lower wall, 4=upper wall
// curve_role:
//   1 = packing_curve  : used to derive reservoir packing and piston travel
//   2 = boundary_curve : collision boundary, ignored for reservoir packing
curve_wall_segments = (
    [2.0, 18.0, 0.0, 0.0, 2.0,  0.0, 0.0, 0.0, 3.0, 1.0],
    [20.0, -18.0, 0.0, 0.0, 8.0,  0.0, 0.0, 0.0, 4.0, 1.0],
    [20.0, 5.0, 0.0, 0.0, 2.0,  1.25, 0.0, 0.0, 3.0, 2.0],
    [25.0, -5.0, 0.0, 0.0, 6.75, 1.25, 0.0, 0.0, 4.0, 2.0]
);

// --------------------------------------------------------------------------
// Particle geometry and integration
// --------------------------------------------------------------------------
radius 		= 0.1;

// Simulation timestep used by both dynamics integration and wave-frame spacing.
dt 			= 0.5;

// --------------------------------------------------------------------------
// Front-end study and output selection
// --------------------------------------------------------------------------
// Normal single-run mode. When study is true, TabFormGenData iterates
// num_studies and creates a separate inline study run for each index.
study 		= false;
num_studies = 0;

output_file_prefix = "TestReservoirPy";
