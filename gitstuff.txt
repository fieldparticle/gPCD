git commands 

cd c:/_DJ/gPCD
git status
git add .
git commit -a -F cmt_msg.txt
git push

// List previous checkins
git log --oneline

// show file name chnages
git diff --name-only

problem observed → tempting fix rejected → convention/rule clarified → exception justified → implementation summarized → result captured.