\begin{tikzpicture}[
    node distance=0.34in and 0.72in,
    box/.style={draw=black!60, fill=black!3, rounded corners=2pt,
        minimum width=2.25in, minimum height=0.38in, text width=2.08in,
        align=center, font=\sffamily\scriptsize},
    stage/.style={box, fill=green!8, draw=green!40!black, line width=0.65pt},
    marker/.style={box, fill=violet!7, draw=violet!55!black},
    constraint/.style={box, fill=orange!10, draw=orange!60!black},
    flow/.style={-{Stealth[length=5pt]}, draw=black!65, line width=0.5pt}
]
\node[stage] (snapshot) {Immutable frame-start snapshot};
\node[box, below=of snapshot] (targets) {Occupancy/direct target scan};
\node[box, below left=of targets] (particles)
    {Mobile target\\particle contact};
\node[marker, below right=of targets] (markers)
    {Boundary marker\\dispatch evaluator from \texttt{Data.z}};
\node[marker, below=of markers] (wall)
    {Evaluate local wall segment\\deduplicate by physical wall flag};
\node[stage, below=2.45in of snapshot] (sum)
    {Complete source-local \texttt{totalForce}\\no target writes};
\node[stage, below=of sum] (candidate)
    {Force-driven candidate velocity};
\node[constraint, below=of candidate] (maximum)
    {Current-state maximum compression\\particle and wall contact set};
\node[stage, below=of maximum] (position)
    {Accept source velocity and integrate position};
\node[box, below=of position] (diagnostics)
    {Diagnostics and distinct error reporting};

\draw[flow] (snapshot) -- (targets);
\draw[flow] (targets) -- (particles);
\draw[flow] (targets) -- (markers);
\draw[flow] (markers) -- (wall);
\draw[flow] (particles) |- (sum);
\draw[flow] (wall) |- (sum);
\draw[flow] (sum) -- (candidate);
\draw[flow] (candidate) -- (maximum);
\draw[flow] (maximum) -- (position);
\draw[flow] (position) -- (diagnostics);
\end{tikzpicture}
