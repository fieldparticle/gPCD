These are the session rules for Field Particle Method

Always assume code changes are for Python only.
Never modify c++ code without asking.
The shader code is always updated at every run.
Do not try to compile c++ code
Do not run tests, just tell me what tests need to be run.

Docments have been moved to: C:\_DJ\gPCDDocs\Dynamics\main.tex
Update yourself on these docuements.

Pay special attentioin to definitions of :
"update project"
"git message"
".venv location"


