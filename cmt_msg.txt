Current status:
    - Vulkan CD-nozzle/reservoir frame hesitation was traced to workload and pacing rather than Python UI behavior.
    - The particle-cell occupancy convention is clarified: particle ID 0 is the empty occupancy sentinel, while CornerList.ploc must continue to use npos because cell index 0 is a valid cell.
    - CommandParticleGraphics clears both the collision-index buffer and lock buffer before vertex occupancy rebuild, so stale clink data is not the primary zero-termination concern.
    - Lowering MAX_CELL_OCCUPANY improved frame pacing because it reduced both dense-cell scan work and the duplicate-target array initialized per source.
    - Constant collision_stiffness_q is now identified as a material weakness in dense compressor/nozzle states: sparse contacts can rebound, but dense pressure fields need resistance to rise with penetration depth.
    - The approved model direction is reversible depth-dependent stiffness without adding energy as dynamic state.

Code Changes:
    - Updated ProblemEvolution.tex with the Vulkan frame-pacing and contact-workload investigation.
    - Documented the zero/npos sentinel split for particle occupancy slots and source corner-cell references.
    - Documented the intended Vulkan compute cleanup: scan local occupancy once, store compact source-owned contact records, and reuse those records for force and remaining-depth checks.
    - Updated DynamicsTheory.tex to describe depth-dependent effective stiffness as the approved material direction while keeping the model momentum-governed.
    - Recorded that ordinary contact force should remain reversible and based on current physical penetration; max-depth containment should remain a safety limit, not the primary material response.

Next:
    - Implement depth-dependent contact stiffness in Python first, then export the same force law to ForceDynamicsSimple GLSL.
    - After that, reduce Vulkan pacing/workload cost by removing ordinary per-frame device waits and replacing the second occupancy scan with cached source-owned contact records.
