Current status:
	- The leakage-response experiments are backed out of the active path: the signed parametric wall penetration change and rasterized marker generation should not be treated as the next model.
	- Boundary markers are being re-centered conceptually as broad-phase sentinels: they identify cells near a wall, while the wall evaluator owns physical position, normal, penetration, and force response.
	- The next wall model should be function-wall based for the active 2D work: y=f(x) segments with configured outward normals, plus a vertical-wall special case.
	- Vulkan should stay unchanged until the Python function-wall behavior is validated.
Code Changes:
	- Documented the function-wall boundary direction in docs/Dynamics/ProblemEvolution.tex.
	- Captured the required implementation order: cleanup first, Python function-wall changes second, Python validation third, Vulkan/export changes last.
	- Preserved the current simple/reservoir checkpoint notes, including dead-particle vertex filtering and ParticleNumber error diagnostics.
Next:
	- Clean up the active wall configuration/evaluator shape.
	- Implement and validate function-wall response in Python, especially small particles at the converging transition.
	- After Python passes, update the simple GLSL exporter/templates and rerun Vulkan captures.
