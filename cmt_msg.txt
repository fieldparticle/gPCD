Current status:
    - CDNozzle reservoir authoring now uses a stream-owned velocity contract.
    - Material display diagnostics now support point size, collision colors, and HSV capture-angle masks.

Code Changes:
    - Documented stream_velocity as the active CDNozzle reservoir velocity field.
    - Documented piston velocity as generated from the reservoir stream for Vulkan compatibility.
    - Documented material-owned point_size, collision_color, non_collision_color, and capture_angleN fields.
    - Documented Vulkan shader material fields for capture angle masks and display diagnostics.

Next:
    - Regenerate CDNozzle data and rebuild Vulkan shaders.
    - Test CDNozzle_Vk with material 0 when validating HSV capture-angle output.