Start Geo dynamics path and document instantaneous geometry direction

- Add GeoBase, GeoDynamics, GeoRunner, Reporting, and InLineTest scaffolding
- Add cfg-driven particle loading with AttrDict-style field access
- Add GLSL-shaped particle fields, push constants, constants.glsl loading, and error reporting
- Add position double buffering with PosLocA/PosLocB and ShaderFlags.positionBuffer
- Add GeoRunner UI for drawing particles, walls, overlap regions, labels, and errors
- Add per-particle CSV reporting using RUN_CONFIGURATION.rpt_frames and run_debug_dir
- Add contact detection, overlap-area calculation, tunneling guardrail, and current-frame contact lists
- Document Geo conventions, file boundaries, naming rules, and GLSL-portability goals
- Update ParticleGeometry.tex for the instantaneous, purely geometric model direction
- Back out the stored-momentum reservoir collision prototype
- Establish that future Geo collision work should avoid persistent contact memory, 
	phase history, first-contact velocity, and global writable shared state