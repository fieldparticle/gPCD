Current status:
    - Python simple and reservoir dynamics now use the generic function-wall model with boundary particles as locality sentinels.
    - TestCompressor is the active closed-end piston compression test for reproducing crowding and max-depth failures.
    - The Python UI now has particle-diameter grid diagnostics and piston status/readout diagnostics for verifying physical scale and piston motion.
    - Runtime cfg normalization now supports both source cfg and generated .tst piston forms.
    - The current compressor failure is understood as rigid piston overcompression: source 1 reaches max-depth constraint against piston wall 1 before particle-particle compression fails.

Code Changes:
    - Updated Python viewers to draw particle radius from the authoritative runtime Data.x field and to preserve world-unit aspect ratio.
    - Added grid_on display support with grid spacing equal to one particle diameter.
    - Added piston line/status diagnostics in the Python UI, including position and velocity near the first mobile particle.
    - Normalized generated piston_velocity_x/y/z fields into piston_velocity for runtime dynamics.
    - Derived missing piston_x_start and piston_x_stop from reservoir boundary curves when running source cfg files directly.
    - Inferred piston_enabled from piston_velocity when the source cfg omits the explicit flag.
    - Corrected Python piston contact so the analytic piston acts at the displayed plane instead of at an offset ghost-particle position.
    - Documented piston runtime normalization, plane-contact behavior, UI scale diagnostics, and the compressor max-depth finding.
    - Updated dynamics theory to distinguish target containment depth from hard fatal compression depth.

Next:
    - CD nozzle.
