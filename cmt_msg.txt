Current status:
	- LightingBall photon streams now own radius and particle separation.
	- Stream wave timing is documented as a physical no-overlap calculation.
	- Vulkan LightingBall captures were verified against generated beam layout.
	- FPML capture decoding now has documented LightingParticle stride requirements.

Code Changes:
	- Updated dynamics docs for per-stream radius/separation and frames_per_wave.
	- Added LightingBall Vulkan beam verification notes.
	- Added FPML LightingParticle ABI/capture guidance.
	- Added TODOs for capture-reader layout detection and spacing_factor cleanup.

Testing:
	- Not run per project rules.
	- Recommended: regenerate LightingBall Py/Vk, run the Python simulation, then run Vulkan LightingBall and verify captures decode with the 256-byte LightingParticle layout.