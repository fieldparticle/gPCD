Current status:
	- Reflecting wall reflection now uses per-photon point splats indexed by SourceID.
	- Photon beam no longer creates reflecting-wall sparkles.
	- ReflectingWallLightMap is no longer the visible reflection path and is now a cleanup candidate.

Code Changes:
	- Documented the particle-owned splat path and old image SSBO status.
	- Updated TODO to remove or isolate the old reflecting-wall light-map SSBO.
	- Preserved the current photon-splat baseline for future tuning.

Next: Remove or gate the old ReflectingWallLightMap SSBO, then tune splat radius/alpha.