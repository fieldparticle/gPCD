Current status:
	- LightingBall is back on a simple sphere-coloring baseline after backing away from text experiments.
	- Material 1 now uses matching orange color, spectral_response, and spectral_emission values.
	- This is documented as base coloring so we can return to it if later text, OBJ, glTF, or boundary-marker work breaks the lighting path.

Code Changes:
	- Updated LightingBall sphere material color fields to [1.0, 0.18, 0.0].
	- Documented the base-coloring checkpoint in LightingBall docs.
	- Added a ProblemEvolution entry for the LightingBall base coloring checkpoint.

Next: Verify the orange sphere lighting in Vulkan, then add any text work back one isolated feature at a time.