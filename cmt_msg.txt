Improve GeoRunner capture diagnostics

- Clear stale CSV report files when starting a new reporting run
- Move GeoRunner CSV writes out of the draw path so paused redraws do not duplicate frames
- Add fixed-width UI reporting for frame, sim time, total momentum, kinetic energy, and particles
- Add momentum and kinetic-energy diagnostics to CSV captures
- Trim particle capture CSVs to currently reported fields
- Remove noisy per-frame export prints from Reporting
- Clear Cap*.csv files from legacy capture directories on startup