Add Geo dynamics scaffold and first reservoir collision prototype

- Add GeoBase/GeoDynamics/GeoRunner separation for the new GLSL-shaped path
- Add cfg-driven particle creation with AttrDict-style field access
- Add position double buffering using PosLocA/PosLocB and ShaderFlags.positionBuffer
- Add source-owned contact slots, contact detection, overlap-area reporting, and tunneling guardrail
- Add GeoRunner drawing for particles, walls, overlap regions, particle labels, and error display
- Add per-particle CSV reporting with frame ranges from RUN_CONFIGURATION.rpt_frames
- Add constants.glsl loading for GLSL-style error/status constants
- Document Geo conventions and current stiffness/alpha_zero collision notes
- Prototype stored-momentum collision handling with separate stored/applied momentum fields

Note: the stored-momentum reservoir prototype exposed model complexity around persistent contact state. 
Next direction is to replace it with an instantaneous, purely geometric collision 
model with no stored contact momentum.