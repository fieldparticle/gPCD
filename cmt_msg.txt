Current status:
	- Added CDNozzle boundary material split between marker particles and visible OBJ geometry.
	- Added OBJ vertex color support so generated boundary meshes can carry visual material color.
	- Added INTERNAL_MOMENTUM / GRAY_LINEAR diagnostic display path and documented that stored momentum is not yet released back to velocity.
	- Updated CDNozzle docs and ProblemEvolution with the approved boundary/material/internal-momentum state.

Code Changes:
	- Parse boundary_particle_material_id and boundary_visual_material_id with material_id fallback.
	- Write generated boundary OBJ vertex RGB and MTL/usemtl material data.
	- Extend Vulkan OBJ loading to read optional vertex RGB and feed CartVert.color.
	- Switch boundary pipeline blending so opaque black boundaries render over white background.
	- Accumulate contact impulse into parms.yzw for internal momentum diagnostics.
	- Add material start_mom/end_mom and GRAY_LINEAR shader mapping.

Next: Define the physical release rule that converts stored internal momentum back into particle velocity.