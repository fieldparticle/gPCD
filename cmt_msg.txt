Current status:
	- ParticleLighting uses indexed runtime lighting surfaces with shared vertices.
	- Per-object deposit_radius is available as a surface smoothing knob.
	- Shadow-wall smoothing still benefits more from higher photon density than deposit-radius widening.
	- Indexed OBJ export is deferred so Blender/debug inspection can later match runtime topology.

Code Changes:
	- Added scene_model deposit_radius parsing and .tst export.
	- Added depositRadius to generated LightingSurfaceObjectMetadata.
	- Updated FPML wall/sphere deposit to use per-object deposit radius.
	- Documented indexed runtime surface state and added TODO for indexed OBJ output.

Next: Increase photon_beam density and later add indexed OBJ export for generated lighting surfaces.