But TwoParticleChaseWall.cfg has drift
TwoParticleChase.cfg boundary violation, but TwoParticleChaseWall does not.
Activated the run_debug flag to tesif p_zero exceeds the max depth and if there is any drift.
For next version--
TwoParticleChase.cfg boundary violation is due to a high relative velocity. We are 
goingto consider that at some depth the particle becomes a solid. New rule
1. Compute velocity-implied p_zero_depth.
2. If p_zero_depth <= geometric cap:
      use normal force-law p_zero.
3. If p_zero_depth > geometric cap:
      clamp p_zero_depth to cap.
      compute capped p_zero_area.
      compute capped p_zero_momentum from the geometry/area rule.
      report that the cap occurred.



