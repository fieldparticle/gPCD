Add velocity-progress shadow response diagnostics

Implement a shadow-only parabolic velocity-progress response in
ShadowDynamics. Store a per-contact velocity reference, compute target
internal momentum from relative-normal velocity progress, and add predicted
compression/rebound seed speeds so head-on contacts produce nonzero shadow
dynamics.

Add a totals panel to MomentumDiagnostics so plot windows show frame/time,
momentum balance, internal momentum, and KE drift without needing the UI.

Update GeoConventions, ParticleGeometry, and ProblemEvolution to record the
move away from geometric p-zero compression control, the fixed
TwoParticleHorizontal shadow startup issue, and the remaining long rebound
tail.

Next: fix rebound strength/timing so velocity-progress shadow contacts return
stored internal momentum cleanly.