Stabilize Geo rebound with source-owned internal momentum

Add a GLSL-compatible internal momentum ledger to Geo contact slots so rebound
cannot release more momentum than compression stored. Preserve the source-owned
execution model by avoiding pairwise write-back while fixing the symmetric
velocity drift seen in TwoParticleHorizontal.

Regenerate GeoDynamics.glsl from the Python export path and update the particle
contact-state comments to document aux.z as stored internal normal momentum.
Refresh the dynamics documentation with current particle-geometry equations,
Geo conventions, export structure, and a ProblemEvolution history entry for the
velocity-drift fix.