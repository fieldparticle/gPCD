Current status:
    - Depth-dependent contact stiffness has been implemented in Python and exported into the shared simple GLSL dynamics path.
    - TestReservoir showed a major stability improvement after the GLSL update, confirming that constant collision_stiffness_q was too weak for dense reservoir compression.
    - The improvement is in the common contact-force path, not a reservoir-specific patch, so it applies to particle-particle, function-wall, and piston contacts that use ForceDynamicsSimple.glsl.
    - Vulkan frame hesitation remains understood as a separate workload/pacing issue: dense cells increase occupancy scan cost and duplicate-list initialization cost.

Code Changes:
    - Added depth-dependent effective stiffness to the simple GLSL exporter.
    - Regenerated ForceDynamicsSimple.glsl and TestPythonBoundarySpheresP.comp from the exporter.
    - Updated ProblemEvolution.tex with the TestReservoir validation result and the remaining Python/GLSL synchronization issue.
    - Preserved the source-owned contact force path and maximum-depth containment behavior.

Important Follow-up:
    - The GLSL stiffness shape currently uses fallback defaults:
        gain = 8.0
        power = 2.0
    - Since TestReservoir looks dramatically better with those values, generated GLSL should next read compression_stiffness_gain and compression_stiffness_power from configuration output so Python and Vulkan stay locked together.

Next:
    - Export compression_stiffness_gain and compression_stiffness_power into local GLSL metadata instead of relying on ForceDynamicsSimple fallback defines.
    - Continue CDNozzle testing on the same shared simple dynamics path.
    - Later, reduce Vulkan workload by replacing the second occupancy scan with cached source-owned contact records.
