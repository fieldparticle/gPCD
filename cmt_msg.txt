Implimented 1:
1. Compute velocity-implied p_zero_depth.
2. If p_zero_depth <= geometric cap:
      use normal force-law p_zero.
3. If p_zero_depth > geometric cap:
      clamp p_zero_depth to cap.
      compute capped p_zero_area.
      compute capped p_zero_momentum from the geometry/area rule.
      report that the cap occurred.
	  
Cleaned up UI to show only velocity.
Added wall_contact_offset to cfg files for ghost wall offset.
Implimented 1 did not work becasue we were not allocating all of the
Incomeing momentum back into the new area. Fixed.

New issues:TwoParticleChaseWall, ThreeParticleChase,TwoParticleStuckWall have drift.




