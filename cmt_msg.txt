Add basic symmetric Geo multi-contact response

Implement source-owned multi-contact collision handling by weighting each
active contact by overlap area and capping compression with both source and
target contact context. Keep one internal momentum ledger per contact slot,
sum contact impulse vectors, and apply one source velocity update.

Regenerate GeoDynamics.glsl from the Python export path and update the particle
geometry documentation for the new weighted multi-contact rule. Add
contacts.csv capture output plus per-particle internal_mom reporting so
multi-contact frames can be cross-referenced with momentum and particle CSVs.

Require run_debug_dir for GeoRunner capture output and add explicit capture
paths for ThreeParticle and ThreeParticleChase. Document the change in
ProblemEvolution as basic symmetric multi-particle functionality.